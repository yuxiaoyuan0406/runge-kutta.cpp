#include "data_type.h"

// template <typename SizeType, typename ValueType>
// inline ValueType &array<SizeType, ValueType>::operator[](SizeType index)
// {
//     if (index >= size) {
//         throw std::out_of_range("Index out of range");
//     }
//     return val[index];
// }

// template <typename SizeType, typename ValueType>
// inline const ValueType &array<SizeType, ValueType>::operator[](SizeType index) const
// {
//     if (index >= size) {
//         throw std::out_of_range("Index out of range");
//     }
//     return val[index];
// }
